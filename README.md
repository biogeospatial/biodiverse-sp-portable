# biodiverse-sp-portable
Strawberry Perl portable shells for Biodiverse usage 

This avoids the need to recalculate a huge array of dependencies for CI runs,
especially when the cache has expired or does not trigger for some reason.

It also simplifies the dependency chain for BiodiverseR.
